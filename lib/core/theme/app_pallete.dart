import 'package:flutter/material.dart';

class AppPallete {
  static const Color backgroundColor =
      Color.fromRGBO(10, 20, 40, 1); // Dark navy blue
  static const Color gradient1 =
      Color.fromRGBO(0, 77, 64, 1); // Dark green (teal-ish)
  static const Color gradient2 =
      Color.fromRGBO(46, 125, 50, 1); // More green (moro green / dark olive)
  static const Color gradient3 = Color.fromRGBO(139, 195, 74, 1);
  static const Color borderColor = Color.fromRGBO(52, 51, 67, 1);
  static const Color whiteColor = Colors.white;
  static const Color greyColor = Colors.grey;
  static const Color errorColor = Colors.redAccent;
  static const Color transparentColor = Colors.transparent;
}
